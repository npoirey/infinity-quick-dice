language: node_js
node_js:
  - "node"

cache: npm

script:
  - npm run build
  - cp bakedresults/*.json dist/

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: false
  local_dir: dist # folder to upload
  on:
    branch: master